node_modules
dist
dist-electron
dist-renderer
release
data
*.log
.DS_Store
.env
.env.local
.vite

# Local Claude Code settings (machine-specific, not shared)
.claude/settings.local.json

# Stray Vite build artifacts that leaked to the repo root.
# The real renderer entry is renderer/index.html — these are stale outputs.
/index.html
/index-*.css
